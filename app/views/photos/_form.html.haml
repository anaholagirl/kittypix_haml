= form_for @photo, :url => user_photos_path, :html => { :multipart => true } do |f|
  %p
    = hidden_field_tag "photo[current_user.id]", current_user.id
    = f.file_field :image
  %p= f.submit "Submit Photo", :class => 'btn btn-primary'
